import {PokeDetailProps} from "@/components/UI/PokeDetail/pokeDetailProps";

export const PokeDetail = ({img, name, type, weight, height, description}: PokeDetailProps) => {
    return (
        <div>

        </div>
    )
}